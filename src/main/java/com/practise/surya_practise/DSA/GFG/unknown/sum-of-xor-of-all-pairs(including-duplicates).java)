/*arr={5,9,7,6} then sum-of-xor-of-all-pairs(including duplicates)=(5+5)^(5+9)^(5+7)^(5+6)^(9+5)^(9+9)^(9+7)^(9+6)^(7+5)^(7+9)^(7+7)^(7+6)^(6+5)^(6+9)^(6+7)^(6+6)
*/
import java.io.*;
import java.util.*;
class Main {
    
    // Driver code
	public static void main (String[] args) throws IOException{
		// Taking input using buffered reader
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		
		int testcases = Integer.parseInt(br.readLine());
		
		// looping through all testcases
		while(testcases-- > 0){
		    String line = br.readLine();
		    String[] element = line.trim().split("\\s+");
		    int sizeOfArray = Integer.parseInt(element[0]);
		    
		    int arr [] = new int[sizeOfArray];
		    
		    line = br.readLine();
		    String[] elements = line.trim().split("\\s+");
		    for(int i = 0;i<sizeOfArray;i++){
		        arr[i] = Integer.parseInt(elements[i]);
		    }
		    
		    
		    Solution obj = new Solution();
		    long res = obj.sumXOR(arr, sizeOfArray);
		    System.out.println(res);
		}
	}
}
class Solution{
   
    // Function for finding maximum and value pair
    public long sumXOR (int arr[], int n) {
        /*int bits[]=new int[31];
        for(int i=0; i<n; i++)
        {
            int currNo=arr[i], index=0;
            while(currNo>0)
            {
                int add=currNo%2;
                currNo/=2;
                bits[index]=bits[index]+add;
                index++;
            }
            //print(bits);
        }
        int noOfZeroes=0,noOfOnes=0;
        long answer=0L;
        for(int i=0; i<31; i++)
        {
            noOfOnes=bits[i];
            noOfZeroes=n-noOfOnes;
            answer=answer+(noOfZeroes*noOfOnes)*(1L<<i);
        }*/
        long answer=0L;
        for (int i=0;i<n; i++ )
        {
            answer=answer+arr[i];
        }
        return answer*2L;
    }
    
    
}


